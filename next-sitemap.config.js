/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://maxlifecenter.org/',
  generateRobotsTxt: true, // generates robots.txt
  sitemapSize: 5000,       // optional, for large sites
};
